{
  "name": "think",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ think",
    "create-local": "graph create --node http://localhost:8020/ think",
    "remove-local": "graph remove --node http://localhost:8020/ think",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 think",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.98.1",
    "@graphprotocol/graph-ts": "0.37.0",
    "graphql": "^16.12.0",
    "graphql-request": "^7.3.5"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}
